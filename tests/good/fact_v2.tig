/* define a recursive function */
let
  /* calculate n! */
  function fact (n : int) : int =
    if  n = 0
      then 1
      else n * fact (n - 1)
in
  (if fact(2) = 2 then print("true") else print("false"); 0)
end
