let
	var a:int := 3
	var b:int := 4

	var z:int := 10 /* escaped var: needs to be kept in memory */

	function sum(x:int, y:int):int = x - y - z
in
	(if sum(a,b)=-11 then print("true") else print("false");0)
end